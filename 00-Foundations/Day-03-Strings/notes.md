# Notes

Strings are immutable in Python.

Important operations:

- Indexing → O(1)
- Traversal → O(n)
- Concatenation → O(n)
- Slicing → O(n)

Interview Tips:

- Prefer ''.join() over repeated '+' inside loops.
- Understand ASCII vs Unicode.
- Know when to use dictionaries for frequency counting.